# 발표자료 구성

## 발표 순서

1. AI 활용사례 소개
2. 왜 이런 최적화(자동화)가 필요한가
3. 아키텍처
4. 단계별 실행 예시 (형상 + 변수 부위 → 아키텍처 각 단계 캡처)
5. 결론

---

## 1. AI 활용사례 소개

우리 팀에서 예측모델 기반 수냉식 냉각판 설계를 하고 있음 (하나의 적용 예시)

- 오늘 발표는 "최적화 결과"가 아니라 "**이 자동화 시스템을 AI와 함께 어떻게 만들었는가**"에 대한 것
- 냉각판은 하나의 예시일 뿐 — 변수 조합이 많아서 전수탐색이 안 되는 설계 문제 전반에 적용 가능한 방식

---

## 2. 왜 이런 최적화(자동화)가 필요한가

일반론으로 — 특정 예시(냉각판)에 종속시키지 않기

- 설계에는 바꿀 수 있는 변수가 여러 개 있고, 변수 조합이 많아지면 하나하나 다 만들어서 해석해보는 게
  현실적으로 불가능해짐
  - (참고 수치, 발표에선 개념만) 변수 2개일 땐 격자를 촘촘히 나열해도 수만 개 수준이라 전수탐색이
    됐지만, 변수가 7개로 늘면 같은 방식으로는 나열 자체가 불가능한 규모가 됨
- 사람이 몇 개만 찍어보고 "이 정도면 됐다"로 끝내면, 왜 그 값으로 정했는지 근거가 안 남고
  최적점을 놓칠 수도 있음
- 그래서 일부만 실제로 실험/해석하고, 예측모델(대체모델)로 나머지 조합까지 추정해서 설계공간
  전체를 파악하는 방식이 필요함 — 예측이 충분히 정확해지면 자동으로 멈추기 때문에, 몇 번을
  돌릴지도 사람이 감으로 정하지 않아도 됨
- 이번엔 이걸 수냉식 냉각판 설계에 적용해본 것 (하나의 적용 예시)

**보완 — "예측모델"이 구체적으로 뭘 하는지 한 문장씩**
- 대체모델: 가우시안 프로세스 회귀(GPR). 예측값과 함께 "이 예측을 얼마나 못 믿는지"(불확실성)도 같이 줌
- 그래서 다음 실험을 어디서 할지도 사람이 정하지 않음 — 불확실성이 가장 큰 지점을 시스템이 스스로 제안
- "충분히 정확해지면 자동으로 멈춘다" = 최근 몇 회 연속으로 예측이 실측과 거의 같으면 종료

---

## 3. 아키텍처 (완성)

- 사용자가 Claude한테 바이브코딩을 요청 → Python 스크립트를 받음 → 복붙해서 내부망에서 사용
- 개발환경: VS Code
- Python을 통해 SolidWorks에서 형상변경을 숫자만 바꾸면 되도록 수식(Equation Manager) 기능으로 구현
- 형상을 바꾸면 모델 업데이트 및 STEP 파일 저장
- Ansys AEDT에서 STEP 파일을 import해서 해석 실행
- 알아야 할 점: SolidWorks로 넘어가는 형상변수(예: 유로각도, 두께)는 설정한 "실험점"이고,
  이 실험점은 OLHD와 ML 코드가 제시함

**보완 — 전체 루프 다이어그램 (슬라이드용)**

```
① OLHD/ML 코드가 "다음에 볼 실험점"(변수 조합)을 제시
        ↓
② Python이 SolidWorks Equation Manager의 수식값을 그 실험점으로 갱신
        ↓
③ 형상 자동 리빌드 → STEP 파일 저장
        ↓
④ Ansys AEDT(Icepak)가 STEP을 import → 경계조건 자동 설정 → 해석 실행
        ↓
⑤ 해석 결과 CSV를 Python이 파싱 → 목적함수·제약조건 값 계산
        ↓
⑥ 그 값을 예측모델(GPR)에 학습 → 다음 실험점 갱신 → ①로 반복
        ↓
⑦ 예측이 연속으로 충분히 정확해지면 자동 종료
```

**보완 — 모듈 구성 (코드 파일 단위, 슬라이드엔 이름만)**
| 모듈 | 역할 |
|---|---|
| OLHD | 최초 실험점 설계 (설계공간을 고르게 커버) |
| Solidworks 자동화 | 수식값 갱신, 리빌드, STEP 저장, 질량 추출 |
| AEDT/Icepak 자동화 | 형상 import, 경계조건 설정, 해석 실행, 결과 CSV 저장 |
| 결과 파싱 | CSV → 목적함수/제약조건 값으로 환산 |
| ML(GPR) | 학습, 다음 실험점 제안(불확실성 최대 지점), 종료 판정 |
| 메인 루프 | 위 전체를 반복 실행 + 실패점 방어(형상이 성립 안 되는 조합 처리) |

- 이 모듈 구성 자체를 처음부터 끝까지 Claude와의 대화로 설계·구현·검증함
- 반복적인 API 호출 코드뿐 아니라, "이 값을 목적함수로 볼지 참고값으로만 볼지" 같은
  **설계 판단 단계**부터 같이 논의하며 진행

---

## 4. 단계별 실행 예시 (준비 필요)

- 수냉식 냉각판 형상 이미지를 띄움
- 최적화 대상 변수(유로 각도, 두께)가 형상의 어느 부위인지 표시
- 앞서 정리한 아키텍처 흐름을 기준으로, 각 단계(VS Code → SolidWorks → Ansys AEDT)를
  실제 화면 캡처로 예시를 들어 진행

**확인 필요**
- [ ] 단계별 화면 캡처(VS Code, SolidWorks Equation Manager, AEDT 실행 등)는 사내망 환경이라
      사용자가 직접 캡처해서 전달 필요

**★ 보완 — 이 단계에 끼워 넣을 사례 (실제 실행 중 있었던 일)**

이 단계별 흐름을 실제로 돌려보던 중, 결과 검증 과정에서 AI가 데이터 이상을 잡아낸 사례가 있었음.
캡처 화면 흐름(④ 해석 → ⑤ 결과 파싱) 사이에 넣으면 자연스러움:

```
증상: 결과 CSV의 특정 측정값이 도면상 계산과 안 맞음
     → 같은 방식으로 뽑은 7개 측정값 중 1개만 유독 어긋남
     → AI와 함께 "왜 이것만 다른가"를 데이터로 추적
     → 원인: 측정 기준면이 유로 경계에 걸쳐서, 메시가 벽면까지 포함해버림 (해석 설정 문제)
     → 측정 위치를 유로 안쪽으로 이동해 수정
결과: 수정 전/후 값이 약 2배 차이 (예: 11.5% → 21.2%)
     → 이미 이 잘못된 값을 근거로 설계 변수 범위를 좁히는 판단까지 내린 상태였음
```

- 이걸 넣으면 4번 파트가 단순 "화면 캡처 나열"이 아니라, **AI가 실제로 검토자 역할을 한 순간**을
  보여주는 파트가 됨 → 5번 결론(더 많이 지원해달라)의 근거가 바로 이 자리에서 마련됨

---

## 5. 결론

바이브 코딩만으로도 이런 구현이 가능했다. 그러니 내부망에 고도화된 에이전트 모델을 구축하거나
누구나 접근하기 쉽게 해주면, 복잡한 최적화 설계를 하는 이 과정을 에이전트 모델이 맥락을 알고
지속적으로 실시간 팔로업해줄 수 있어서 매우 좋은 방향이 될 것 같다

**보완 — 현재 상태를 짚고 넘어가야 이 요청이 더 설득력 있음**

- 사내망에 에이전트 AI 환경이 이미 있음 — 그런데 실제로 써보면 탑재된 언어모델 성능이 낮아서,
  4번에서 보여드린 수준(측정 이상 원인 추적, 설계 판단)의 작업은 잘 안 됨
- 그래서 이번 사례는 결국 **외부 AI(구독형)**로 진행함 — 지금 보여드린 결과물이 그 방식의
  실제 성과물

**요청 — 투트랙**

| | 요청 | 이유 |
|---|---|---|
| **장기** | 사내망 에이전트에 고성능(프론티어급) 언어모델 도입 | 인프라는 이미 있음 — 부족한 건 모델 자체 |
| **단기** | 그 전까지 **외부 AI 구독료 지원 확대** | 이번 성과가 바로 그 방식(구독형 외부 AI)으로 나온 결과이고, 고도화까지는 시간이 걸리므로 그 사이 공백이 생기지 않게 |

> "환경은 갖춰졌지만, 그 환경을 채울 모델이 아직 부족합니다.
>  고도화되는 동안에는, 지금 검증된 방식(외부 AI 구독)을 계속 쓸 수 있게 해주십시오."

---

## 부록 — 예상 질문 대비

| 질문 | 답변 요지 |
|---|---|
| 최적화 최종 결과는? | 아직 파이프라인 구축·검증 단계. 오늘은 그 과정에서 AI가 한 역할에 대한 발표 |
| 왜 GPR/이 방법론인가? | 예측값과 함께 불확실성도 주기 때문에, 다음 실험 지점 선정과 종료 판정이 자동화됨 |
| AI가 틀린 적은 없나? | 있었음. "가설 → 검증 방법 제시 → 확인" 절차를 같이 밟는 방식으로 진행해 원인을 좁혀나감 |
| 사내망 에이전트로는 왜 안 됐나? | 인프라·접근성은 문제없음. 언어모델 자체 성능이 이런 수준의 판단에는 부족했음 |
| 외부 AI 구독료는 얼마나 드나? | (발표 시점 실제 구독료·라이선스 정보로 채울 것) |
| 보안은 어떻게 하나? | 장기적으로 사내망 모델 고도화로 해소. 단기 구독은 기존 보안 검토·승인 절차 내에서 운영 |
