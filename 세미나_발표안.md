# AI 활용사례 — 수냉식 냉각판 유로 최적화

> 발표 PPT 구성용 초안. 슬라이드 단위로 `---` 구분.
> 이 사례는 "결과"가 아니라 **"AI와 함께 ML 기반 최적화 파이프라인을 구축한 예시"**로 씀.
> 유로 형상·목적함수 같은 도메인 디테일은 발표에서 설명하지 않음 — 배경 그림 1장이면 충분.

**핵심 메시지 한 줄**: AI와 함께 데이터 학습(GPR) 기반 자동 최적화 시스템을 만들었고,
그 과정에서 AI가 실제로 설계 오류를 잡아냈다. 다만 망 분리 때문에 그 능력이 절반만 쓰였다.

---

## 슬라이드 1 — 표지

- 제목: AI 활용사례 — 데이터 학습 기반 설계 최적화, 그리고 남은 과제
- 부제: 수냉식 냉각판 유로 최적화 파이프라인 구축 사례
- 발표자 / 소속 / 일자

---

## 슬라이드 2 — 예시 과제 소개 (배경, 30초용)

**말할 것**: "냉각판 유로 형상을 최적화하는 과제를 예로 듭니다. 설계변수 7개, 해석 1회에
수십 분 걸리는 SolidWorks+Icepak 시뮬레이션 기반입니다. 형상 디테일은 중요하지 않고,
**'AI와 함께 ML 최적화 시스템을 구축했다'**는 게 오늘의 요점입니다."

**슬라이드에 넣을 것**
- 냉각판 이미지 1장 (`image.png`)
- 한 줄: 설계변수 7개 → 시뮬레이션 1회 수십 분 → 전수탐색 불가능한 규모

---

## 슬라이드 3 — 접근 방식: 데이터 학습 기반 최적화

**핵심 메시지**: 매번 해석을 돌리는 대신, 해석 결과를 학습해 예측하는 모델(GPR)을 만들고
그 위에서 최적화한다.

**슬라이드에 넣을 것**
```
[1] 실제 시뮬레이션을 반복 실행하며 데이터 축적
[2] 그 데이터로 GPR(가우시안 프로세스) 학습 → "설계값 → 결과"를 예측하는 대리모델 완성
[3] 대리모델 위에서 유전알고리즘으로 최적 후보 탐색 (실제 해석 없이, 비용 거의 0)
[4] 최종 후보만 실제 시뮬레이션으로 재검증
```
- 부가 포인트: 어디를 다음에 실험할지도 AI가 "가장 불확실한 지점"을 골라 자동 선택
  (Active Learning / 적응 샘플링)

**말할 것**: "이게 오늘 보여드리고 싶은 방법론입니다. 실험 예산이 제한적일 때
데이터 학습으로 전체 설계공간을 지도화하고, 그 위에서 저비용으로 탐색하는 방식이
이번 과제뿐 아니라 시뮬레이션 기반 설계 전반에 적용 가능합니다."

---

## 슬라이드 4 — 이 시스템을 AI와 함께 만들었다

**핵심 메시지**: 코드 타이핑이 아니라, 대화로 설계·구현·검증까지 진행했다.

**슬라이드에 넣을 것**
- 파이프라인 구성 (모듈명 나열 정도만, 코드 없이)
```
형상 자동 생성 → 시뮬레이션 자동 실행 → 결과 파싱 → GPR 학습/샘플링 → 자동 반복
```
- 한 줄: 전체 자동화 루프를 AI와 대화로 설계·구현

**말할 것**: "API 호출 코드 같은 반복 작업은 물론이고, '이 목적함수를 어떻게 정의해야
하는가', '이 지표가 신뢰할 만한가' 같은 **설계 판단 단계**부터 AI와 논의하며 진행했습니다."

---

## 슬라이드 5 — ★ AI가 실제로 한 일: 오류 발견

**핵심 메시지**: 자동화 파이프라인 안에서, AI가 실제로 잘못된 데이터를 잡아냈다.

**슬라이드에 넣을 것**
- 상황 요약 (숫자만 강조, 원인 설명은 발표에서 구두로 짧게)
```
시뮬레이션 결과값 하나가 실제보다 절반 가까이 낮게 계산되고 있었음
   → 이미 그 값을 근거로 설계 방향(변수 범위)까지 좁혀놓은 상태
   → AI와 데이터를 같이 들여다보며 원인 추적 → 측정 설정 오류 확정 → 수정
```
- Before/After 숫자 하나만 크게: **"11.5% → 21.2%"**

**말할 것**: "이게 오늘 제일 강조하고 싶은 지점입니다. AI는 코드를 짜준 것뿐 아니라,
결과 데이터를 같이 보면서 '이 숫자 이상하지 않냐'는 걸 짚어냈고, 같이 원인을 좁혀서
찾아냈습니다. 못 잡았으면 잘못된 데이터로 최적화 전체가 진행될 뻔했습니다."

---

## 슬라이드 6 — 그런데: 망이 분리되어 있다

**핵심 메시지**: 시뮬레이션은 내부망, AI는 외부망 — 사람이 수작업으로 중개해야 결과를
AI에게 보여줄 수 있다.

**슬라이드에 넣을 것**
```
[외부망] AI와 대화·코드 작성        [내부망] 실제 시뮬레이션 실행
              │                              │
              └────── 사람이 결과를 손으로 복사 ──────┘
```
- 한 줄: "방금 보여드린 오류 발견도, 제가 결과를 일일이 복사해줬기 때문에 가능했습니다."

**말할 것**: "자동으로 수백 번 반복되는 캠페인 중에는, 사람이 매번 결과를 옮겨줄 수
없습니다. 즉 지금 구조에서는 **AI가 검토할 수 있는 건 사람이 보여준 일부뿐**입니다."

---

## 슬라이드 7 — 제안: 사내망 에이전트 AI 환경

**핵심 메시지**: 모델 성능이 아니라 접근 권한의 문제다.

**슬라이드에 넣을 것**
```
[현재]  AI 능력 O   +   데이터 접근 X   =   검토는 사람이 보여준 것만
[제안]  AI 능력 O   +   데이터 접근 O   =   작업 맥락을 실시간으로 팔로업
```
- 요청 사항
```
1. 사내망 내 에이전트 AI 환경 구축 검토
2. 해석 자동화 과제(본 사례 등)를 파일럿으로 지정
3. 팀 차원의 공통 자산화
```

**말할 것**: "지금 능력으로도 오류를 잡아냈습니다. 필요한 건 더 똑똑한 모델이 아니라,
그 능력이 실제 작업 데이터에 상시로 닿을 수 있는 환경입니다."

---

## 슬라이드 8 — 마무리

**핵심 메시지 재확인 (텍스트로 남길 것)**

> "AI가 못 해서가 아니라, 닿지 못해서 못 하고 있습니다."

Q&A

---

## 부록 — 예상 질문 대비

| 질문 | 답변 요지 |
|---|---|
| 최적화 최종 결과는? | 아직 파이프라인 구축·검증 단계. 오늘은 **그 과정에서 AI가 한 역할**에 대한 발표 |
| 왜 GPR/이 방법론인가? | 예측값과 함께 불확실성도 주기 때문에, 다음 실험 지점 선정과 신뢰도 판단이 가능 |
| AI가 틀린 적은 없나? | 있었음. 그래서 "가설 → 검증 방법 제시 → 확인" 절차를 같이 밟는 방식으로 진행 |
| 보안은 어떻게 하나? | 그래서 외부 서비스가 아니라 **사내망 구축**을 제안하는 것 |
