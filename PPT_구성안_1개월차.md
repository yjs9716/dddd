# 1개월차(연장) PPT 구성안 — AI 에이전트 활용방안 및 DOE 결과

> 산출물: AI 에이전트 활용방안 및 DOE 결과 정리자료 (항공플랫폼 설계가이드에 포함)
> 총 14매 구성 / 지난 차수와 동일한 `01·02·03` 목차 포맷 유지

---

## 전체 흐름

```
01  AI 에이전트 기반 개발환경 구축      ← 어떻게 만들었는가 (방법)
02  설계변수 정의 및 설계공간 설정       ← 무엇을 변수로 두었는가 (대상)
03  실험계획법(DOE) 기반 데이터 확보     ← 얼마나 확보했는가 (결과)
```

---

## 슬라이드 1 — 표지

- 제목: **AI 에이전트 활용방안 및 DOE 기반 해석 데이터 확보**
- 부제: 수냉식 냉각판 유로 설계최적화 (1개월차)
- 소속 / 성명 / 일자

---

## 슬라이드 2 — 목차

지난 차수 PPT와 동일한 포맷(좌측 대형 번호 + 우측 세부항목).

| No | 대주제 | 세부 항목 |
|---|---|---|
| **01** | AI 에이전트 기반 개발환경 구축 | 1. AI 에이전트 개발환경 구성<br>2. 기존 자동화 코드 연계 방식<br>3. 코드 생성–검증–반영 절차<br>4. 활용 사례 |
| **02** | 설계변수 정의 및 설계공간 설정 | 1. 유로 형상 및 유동 경로<br>2. 설계변수 선정<br>3. 변수 범위 및 제약조건<br>4. CAD 파라미터 연동 검증 |
| **03** | 실험계획법(DOE) 기반 데이터 확보 | 1. DOE 기법 선정<br>2. 자동 해석 루프 구성<br>3. 해석 실패점 처리<br>4. 결과 데이터 구조화 |

---

## 슬라이드 3 — [01 간지] AI 에이전트 기반 개발환경 구축

- 대형 번호 `01` + 제목
- 한 줄 요약: "해석 자동화·머신러닝 코드를 AI 에이전트와의 협업(바이브코딩)으로 구현"

---

## 슬라이드 4 — AI 에이전트 개발환경 구성

**넣을 것**
- 구성도 (좌 → 우 흐름)

```
[개발자]  ──▶  [AI 에이전트]  ──▶  [생성 코드]  ──▶  [SolidWorks / Icepak 자동화]
     ▲                                                        │
     └────────────── 결과 확인 및 피드백 ◀──────────────────┘
```

- 적용 도구 및 환경 (에이전트 도구, 실행 환경, 버전관리)
- 적용 범위: 형상 제어 / 해석 실행 / 결과 파싱 / ML 학습 코드 전 영역

**발표 포인트**: 단순 코드 자동완성이 아니라, 파이프라인 전체를 대화 기반으로 구현·수정하는 방식

---

## 슬라이드 5 — 기존 자동화 코드 연계 방식

**넣을 것**
- 모듈 구성도 (지난 차수 산출물과의 연결을 명시)

```
[지난 차수 산출물]                    [금번 확장]
프로그래밍 환경 구축      ──▶   설계변수 정의 모듈
솔버 연동 자동제어        ──▶   DOE 샘플링 · 자동 해석 루프
결과 데이터 구조화        ──▶   결과 파싱 · 데이터셋 축적
```

- 설계변수·상수의 **단일 출처(Single Source) 관리 원칙**
  - 변수 정의를 한 곳에만 두고 나머지 모듈이 참조 → 부분 수정 시 불일치 방지

**발표 포인트**: 지난 차수 결과물을 버리지 않고 그대로 확장했다는 연속성 강조

---

## 슬라이드 6 — 코드 생성–검증–반영 절차

**넣을 것**
- 절차 4단계 도식

```
① 요구사항 정의  →  ② AI 코드 생성  →  ③ 검증  →  ④ 반영
   (물리 조건·             (대화 기반)        (단위 확인,      (형상 관리)
    출력 형식)                               물리적 타당성)
```

- 검증 기준 예시
  - 계산값이 물리적으로 타당한 범위인가 (차압, 유량, 온도)
  - 수기 계산값과 일치하는가 (중량 등 검산 가능 항목)
  - 예외 상황에서 의도대로 동작하는가

**발표 포인트**: AI 출력을 그대로 쓰지 않고 검증 절차를 표준화했다는 점

---

## 슬라이드 7 — ★ 활용 사례: 해석 설정 오류 발견

**이번 자료에서 가장 임팩트 있는 슬라이드.**

**넣을 것**
- Before / After 수치를 크게 대비

```
   측정 설정 오류로 특정 유량 지표가 실제보다 낮게 산출
              11.5%   →   21.2%
   (오류 발견 전)        (수정 후)
```

- 발견 경위 3단계
  1. 결과 데이터 검토 중 값의 이상 징후 확인
  2. 원인 가설 수립 및 검증 방법 도출
  3. 측정면 설정 오류로 확정 → 수정 및 재검증

**발표 포인트**: 미발견 시 잘못된 데이터로 최적화 전체가 진행될 뻔했음 → AI 협업의 실질적 효과

---

## 슬라이드 8 — [02 간지] 설계변수 정의 및 설계공간 설정

- 대형 번호 `02` + 제목
- 한 줄 요약: "최적화 대상이 되는 유로 형상 변수와 탐색 범위를 확정"

---

## 슬라이드 9 — 유로 형상 및 유동 경로

**넣을 것**
- 냉각판 형상 이미지 (앞면/뒷면)
- 유동 경로 도식

```
입구 → 하단 유로(1차 통과) → 분기 ┬→ 전원공급모듈
                                   └→ 상단 유로(2차 통과) → 합류 → 출구
```

- 특징: 분기 구조이므로 총 유량이 두 경로로 나뉨 (한쪽이 늘면 반대쪽이 줄어드는 관계)

**발표 포인트**: 단순 직렬 유로가 아니라 분기·재순환 구조여서 변수 간 상호작용이 큼 → 최적화가 필요한 이유

---

## 슬라이드 10 — 설계변수 선정 및 범위 정의

**넣을 것**
- 설계변수 표 (변수명 / 설명 / 범위 / 단위)

| # | 설계변수 | 범위 | 단위 |
|---|---|---|---|
| 1~8 | 유로 두께·각도 계열 변수 8종 | (실제 값 기재) | mm / deg |

- 목적함수 및 제약조건

| 구분 | 항목 |
|---|---|
| 목적함수 | 차압, 유량 분배 균일도(CV), 온도 편차, 최고온도 |
| 제약조건 | 전원모듈 분기 유량비, 총 중량 |

**발표 포인트**: 8차원 설계공간 → 전수탐색 불가 → 데이터 학습 기반 접근이 필요한 근거

---

## 슬라이드 11 — CAD 파라미터 연동 검증

**넣을 것**
- 연동 흐름

```
설계변수 값  →  CAD 전역변수 갱신  →  형상 리빌드  →  STEP 내보내기  →  해석
```

- 검증 항목
  - 변수명 불일치 시 즉시 중단·경고 (형상이 안 바뀐 채 해석되는 사고 방지)
  - 리빌드 후 질량·부피 추출값이 GUI 표시값과 일치함을 확인
  - 중량 산출식이 수기 계산값과 소수점까지 일치함을 검산

**발표 포인트**: "조용히 잘못 도는" 실패 모드를 차단하는 안전장치를 넣었다는 점

---

## 슬라이드 12 — [03 간지] 실험계획법(DOE) 기반 데이터 확보

- 대형 번호 `03` + 제목
- 한 줄 요약: "머신러닝 학습에 사용할 초기 해석 데이터셋 확보"

---

## 슬라이드 13 — DOE 기법 선정 및 자동 해석 루프

**넣을 것**
- DOE 기법 비교 (선정 근거)

| 방식 | 특징 | 채택 |
|---|---|---|
| 격자 전수탐색 | 8차원에서 조합 수 폭증, 불가능 | ✕ |
| 무작위 샘플링 | 공간 편중 발생 가능 | ✕ |
| **최적라틴방격법(OLHD)** | 적은 점으로 설계공간 균일 분포 | **○** |

- 초기 실험점 수: `10 × 변수 수` 경험칙 적용
- 자동 해석 루프 도식

```
DOE 실험점  →  CAD 리빌드  →  Icepak 해석  →  결과 파싱  →  데이터셋 누적
     ▲                                                          │
     └──────────────────  다음 실험점  ◀────────────────────┘
```

**발표 포인트**: 1회 해석에 수십 분 소요 → 무인 연속 운전이 전제 조건

---

## 슬라이드 14 — 해석 실패점 처리 및 결과 데이터 구조화

**넣을 것**
- 예외 처리 3종

| 상황 | 처리 방식 |
|---|---|
| 형상 미성립 (리빌드 실패) | 실패 이력 기록 후 다음 점으로 진행, 해당 영역 재시도 방지 |
| 솔버 비정상 종료 | 자동 재연결 후 동일 실험점 재시도 |
| 연속 실패 | 설정 오류로 판단하고 캠페인 중단 |

- 결과 데이터 구조 (ML 학습 입력 형태)

```
idx | 설계변수 8종 | 목적함수 | 제약조건
```

- 확보 현황: DOE N점 중 유효 M점 확보 (실제 수치 기재)

**발표 포인트**: 사람이 지키고 있지 않아도 도는 구조를 만든 것이 이번 달의 실질적 성과

---

## 슬라이드 15 — 차월 계획

- 확보된 DOE 데이터 기반 머신러닝 대리모델(GPR) 학습
- 예측값·실측값 비교를 통한 정확도 검증
- 설계변수별 민감도 산출 및 지배 변수 식별

---

## 작성 시 유의사항

| 항목 | 내용 |
|---|---|
| 분량 배분 | 01(활용방안) 5매 · 02(설계변수) 4매 · 03(DOE 결과) 3매 — 산출물 명칭이 "활용방안 **및** DOE 결과"이므로 양쪽 비중을 맞춤 |
| 수치 기재 | 슬라이드 10·14의 실제 값은 캠페인 진행 후 확정치로 채울 것 |
| 핵심 슬라이드 | 슬라이드 7(오류 발견 사례) — 유일하게 정성적 성과를 수치로 보여주는 장표이므로 가장 공들일 것 |
| 도메인 설명 | 유로 형상 세부 설명은 최소화 (설계가이드 수록용이므로 방법론 위주로 서술) |
