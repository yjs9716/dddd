# 1개월차(연장) PPT 구성안 — AI 에이전트 활용방안 및 DOE 결과

> 산출물: AI 에이전트 활용방안 및 DOE 결과 정리자료 (항공플랫폼 설계가이드에 포함)
> 기준 형상: 냉각판 V5 — 유로 + 방열핀 9변수 (`cooling-plate_fin-add-version`)
> 총 16매 / 지난 차수와 동일한 `01·02·03` 목차 포맷

---

## 전체 흐름

```
01  AI 에이전트 기반 개발환경 구축     ← 어떻게 만들었는가   (방법)
02  설계변수 정의 및 설계공간 설정      ← 무엇을 변수로 두었나 (대상)
03  실험계획법(DOE) 기반 데이터 확보    ← 얼마나 확보했는가   (결과)
```

---

## 슬라이드 1 — 표지

- 제목: **AI 에이전트 활용방안 및 DOE 기반 해석 데이터 확보**
- 부제: 수냉식 냉각판 유로·방열핀 설계최적화
- 소속 / 성명 / 일자

---

## 슬라이드 2 — 목차

| No | 대주제 | 세부 항목 |
|---|---|---|
| **01** | AI 에이전트 기반 개발환경 구축 | 1. 개발환경 및 협업 방식<br>2. 모듈 구성 및 단일 출처 원칙<br>3. 코드 검증 절차<br>4. 활용 사례 |
| **02** | 설계변수 정의 및 설계공간 설정 | 1. 냉각판 형상 및 유동 경로<br>2. 설계변수 선정 (자유 9 + 고정 2)<br>3. 변수 고정·조정 근거<br>4. 방열핀 배치 및 가공 제약 |
| **03** | 실험계획법(DOE) 기반 데이터 확보 | 1. DOE 기법 선정<br>2. 제약 만족 실험점 생성<br>3. 무인 자동 해석 루프<br>4. 데이터 확보 현황 |

---

# 01. AI 에이전트 기반 개발환경 구축

## 슬라이드 3 — [간지]

- 대형 번호 `01` + 제목
- 한 줄 요약: "형상 제어 · 해석 실행 · 결과 처리 전 과정의 코드를 AI 에이전트와의 협업으로 구현"

---

## 슬라이드 4 — 개발환경 및 협업 방식

**넣을 것**
- 협업 흐름도

```
[설계자] ──요구사항·물리조건──▶ [AI 에이전트] ──생성──▶ [자동화 코드]
    ▲                                                        │
    └──────── 결과 검토 · 이상 징후 피드백 ◀──────────────┘
```

- 적용 범위 (8개 모듈 전 영역)

| 모듈 | 역할 |
|---|---|
| `OLHD` | 설계변수 정의 · DOE 실험점 생성 |
| `fins` | 방열핀 배치 수식 · 가공 제약 판정 |
| `Solidworks` | 형상 변수 갱신 · 리빌드 · STEP 내보내기 |
| `icepak` | 해석 모델 생성 · 측정면 설정 · 솔브 |
| `result_parser` | 결과 CSV 파싱 · 지표 산출 |
| `ML` | 대리모델 학습 · 적응샘플링 · 종료판정 |
| `main` | 캠페인 루프 제어 |
| `grid_convergence` | 격자수렴성시험(GCI) |

**발표 포인트**: 단순 코드 자동완성이 아니라, 지표 정의·검증 방법 같은 **판단 단계부터** 협업

---

## 슬라이드 5 — 모듈 구성 및 단일 출처(Single Source) 원칙

**넣을 것**
- 값이 정의된 위치를 한 곳으로 고정한 구조

| 값 | 정의 위치 | 참조하는 모듈 |
|---|---|---|
| 설계변수 이름·범위, 고정값 | `OLHD` | ML · Solidworks · result_parser |
| DOE 점 수 (10 × 변수 수) | `OLHD` | ML |
| 핀뱅크 길이 · 최소갭 · 갭 공식 | `fins` | OLHD · Solidworks · icepak · result_parser |
| 발열채널 수 · 유로 깊이 | `icepak` | result_parser |

- 원칙: **같은 값을 두 곳에 쓰지 않는다** → 한쪽만 고쳤을 때 조용히 어긋나는 사고 차단
- 적용 예: 핀뱅크 길이를 바꾸면 갭 공식·유로 위치·최대 핀 개수가 자동으로 따라감

**발표 포인트**: AI가 코드를 빠르게 만들수록 일관성 관리가 중요 → 구조 설계로 해결

---

## 슬라이드 6 — 코드 검증 절차

**넣을 것**
- 4단계 절차

```
① 요구사항 정의  →  ② AI 코드 생성  →  ③ 검증  →  ④ 반영
   (물리조건·출력형식)     (대화 기반)      (아래 3종)    (형상관리)
```

- 검증 3종

| 구분 | 방법 | 실제 적용 예 |
|---|---|---|
| 수식 검산 | 수기 계산값과 대조 | 중량 산출식 — 소수점까지 일치 확인 |
| 전수 검증 | 후보 대량 생성 후 제약 위반 여부 확인 | 후보 20만 개 → 갭 제약 위반 0건 |
| 통합 테스트 | 해석 없이 가짜 데이터로 전 루프 구동 | DOE → 적응샘플링 → 종료판정 동작 확인 |

**발표 포인트**: AI 출력을 그대로 신뢰하지 않고 검증을 절차화

---

## 슬라이드 7 — ★ 활용 사례 (1) 제약 위반 버그 발견

**넣을 것**
- 상황 도식

```
설계값 생성 시 반올림 순서 오류
  두께 2.1554 → 허용 핀 개수 18 산출 → 두께가 2.2로 반올림
  → 실제 갭 2.468mm  (가공 제약 2.5mm 위반)
```

- 발견 경로: **통합 테스트의 제약 판정 안전망**이 경계 조합에서 검출
- 조치: 두께를 먼저 반올림한 뒤 허용 개수를 계산하도록 수정 → 후보 20만 개 전수 재검증(위반 0건)

**발표 포인트**: 미발견 시 **가공 불가능한 형상**이 데이터에 섞여 최적화 결과가 오염될 뻔함

---

## 슬라이드 8 — ★ 활용 사례 (2) 해석 설정 오류 발견

**넣을 것**
- Before / After 수치를 크게 대비

```
   측정면 설정 오류로 분기 유량이 실제보다 낮게 산출
              11.5%   →   21.2%
```

- 발견 경위 3단계
  1. 결과 데이터 검토 중 값의 이상 징후 확인
  2. 원인 가설 수립 및 검증 방법 도출
  3. 측정면이 메시 경계에 걸쳐 면적이 왜곡된 것으로 확정 → 수정·재검증

**발표 포인트**: 이 값을 근거로 설계변수 범위까지 이미 좁혀둔 상태였음 → 데이터 신뢰성 확보의 중요성

---

# 02. 설계변수 정의 및 설계공간 설정

## 슬라이드 9 — [간지]

- 대형 번호 `02` + 제목
- 한 줄 요약: "유로 7변수 + 방열핀 2변수, 총 9개 자유변수로 설계공간 정의"

---

## 슬라이드 10 — 냉각판 형상 및 유동 경로

**넣을 것**
- 냉각판 형상 이미지 (앞면 / 뒷면)
- 유동 경로 도식

```
입구 → 하단 유로(1차 통과) → 분기 ┬→ 전원공급모듈
                                   └→ 상단 유로(2차 통과) → 합류 → 출구
```

- 조건: 총 유량 4 LPM 고정, 발열채널 9개 (채널당 40 W)
- 특징: **분기 구조** — 한쪽 경로가 유량을 많이 가져가면 반대쪽은 필연적으로 줄어듦

**발표 포인트**: 직렬 유로가 아니라 분기·재통과 구조여서 변수 간 상호작용이 큼 → 단순 튜닝으로는 최적점을 찾기 어려움

---

## 슬라이드 11 — 설계변수 선정 (자유 9 + 고정 2)

**넣을 것**
- 자유변수 표

| # | 설계변수 | 범위 | 단위 |
|---|---|---|---|
| 1 | `input_thick` | 13 ~ 25 | mm |
| 2 | `input_angle` | 90 ~ 150 | deg |
| 3 | `power_input_thick` | 3 ~ 20 | mm |
| 4 | `mid_thick` | 10 ~ 25 | mm |
| 5 | `mid_angle` | 90 ~ 140 | deg |
| 6 | `mid_input_thick` | 10 ~ 25 | mm |
| 7 | `output_thick` | 13 ~ 35 | mm |
| 8 | `fin_thick` (방열핀 두께) | 1.5 ~ 3.0 | mm |
| 9 | `fin_count` (방열핀 개수) | 10 ~ 21 | 개 (정수) |

- 평가 지표

| 구분 | 항목 |
|---|---|
| 목적함수 (5) | 차압 · 채널 온도편차 · 최고온도 · 1차통과 유량편차 · 2차통과 유량편차 |
| 제약조건 (2) | 전원모듈 분기 유량비 · 총 중량 |

**발표 포인트**: 9차원 → 전수탐색 불가 → 데이터 학습 기반 접근이 필요한 근거

---

## 슬라이드 12 — 변수 고정 및 범위 조정 근거

**넣을 것**
- 고정 변수 2종과 근거

| 변수 | 처리 | 근거 |
|---|---|---|
| `power_output_thick` | 25mm 고정 | 이전 차수 데이터 211점 분석 결과 전 구간에서 목적함수가 같은 방향 → **트레이드오프 없음**. 탐색 예산을 방열핀으로 이전 |
| `fin_height` | 8.0mm 고정 | 유로 깊이와 동일하게 두어 핀 위 우회공간 제거. 가변 시 0.1~2mm의 얇은 틈을 전 실험점에서 메싱해야 하고, 우회유동이 열전달·압력강하에 상반된 영향을 줌 |

- 범위 조정: `input_thick` / `mid_thick` / `mid_input_thick` 상한 축소 (35 → 25mm)
  - 성능이 아니라 **형상 간섭 제약** — 발열채널을 양 끝에 추가하면서 방열핀 배치 공간 확보 필요

**발표 포인트**: 변수를 늘리기만 한 것이 아니라, **근거를 갖고 줄여서** 탐색 예산을 재배분

---

## 슬라이드 13 — 방열핀 배치 및 가공 제약

**넣을 것**
- 배치 도식 및 폐합조건

```
[벽] g [핀] g [핀] g ... g [핀] g [벽]

L = N·t + (N+1)·g        (L = 핀뱅크 길이 86.5mm)
g = (L − N·t) / (N+1)     ← 갭은 종속 계산값
```

- 핀 N개 → 유로 N+1개. 폐합조건 하나로 **벽↔핀 / 핀↔핀 / 핀↔벽 간격이 전부 자동으로 동일**
- 가공 제약: **갭 ≥ 2.5mm** (밀링 가공 한계, 메시 확보)
  - 두께·개수 박스에서 이 조건을 만족하는 영역은 약 70%
  - CAD에서도 갭을 수식으로 걸어 파이썬과 이중 계산하지 않도록 구성

**발표 포인트**: 설계변수 2개가 서로 독립이 아니라 삼각형 유효영역을 이룸 → 샘플 생성 방식에 영향(슬라이드 15)

---

# 03. 실험계획법(DOE) 기반 데이터 확보

## 슬라이드 14 — [간지]

- 대형 번호 `03` + 제목
- 한 줄 요약: "머신러닝 학습용 초기 데이터셋 확보 및 무인 자동 해석 체계 구축"

---

## 슬라이드 15 — DOE 기법 선정 및 제약 만족 실험점 생성

**넣을 것**
- 기법 비교

| 방식 | 특징 | 채택 |
|---|---|---|
| 격자 전수탐색 | 9차원에서 조합 수 폭증 | ✕ |
| 무작위 샘플링 | 공간 편중 발생 | ✕ |
| **최적라틴방격법(OLHD)** | 적은 점으로 설계공간 균일 분포 | **○** |

- 초기 실험점 수: `10 × 변수 수` = **90점**
- 갭 제약 처리 방식

```
[기각 방식]  박스에서 뽑고 → 제약 위반이면 버림  → 후보 30% 낭비 + 층화 붕괴
[채택 방식]  두께에 따라 허용되는 개수 범위로 접어 넣어 생성 → 낭비 0, 층화 유지
```

**발표 포인트**: 제약이 있는 설계공간에서는 샘플 **생성 단계**에서 제약을 반영해야 데이터 품질이 유지됨

---

## 슬라이드 16 — 무인 자동 해석 루프

**넣을 것**
- 루프 도식

```
DOE 실험점 → CAD 변수 갱신·리빌드 → STEP → Icepak 해석 → 결과 파싱 → 데이터셋 누적
      ▲                                                                      │
      └────────────────────  다음 실험점  ◀──────────────────────────────┘
```

- 예외 처리 3종

| 상황 | 처리 |
|---|---|
| 형상 미성립 (리빌드 실패) | 실패 이력 기록 후 다음 점 진행, 해당 영역 재시도 방지 |
| 솔버 비정상 종료 | 자동 재연결 후 동일 실험점 재시도 |
| 연속 실패 | 설정 오류로 판단하고 캠페인 중단 |

- 무인 장기 운전: CAD·해석 프로그램 GUI 비활성화로 장기 연속 구동 확보
- 1회차 소요: 해석 약 17분 → **사람이 붙어 있지 않아도 데이터가 쌓이는 구조**

**발표 포인트**: 이번 달의 실질적 성과 — 데이터 생산 설비화

---

## 슬라이드 17 — 측정 지표 정의 (유량 균일도)

**넣을 것**
- 문제: 방열핀 개수가 설계변수 → 유로 개수가 설계마다 **11~22개로 가변**
  - 대리모델은 출력 차원이 고정이어야 하므로 개수가 변하는 값을 그대로 쓸 수 없음
- 검토했던 대안과 한계

| 방식 | 한계 |
|---|---|
| 대표 위치 몇 점만 측정 | 유로 개수가 짝수면 정중앙이 없어 위치가 어긋남 |
| 양 끝 2점만 측정 | 중앙만 다른 비단조 패턴을 놓침 |

- 채택: **유로 전체를 측정하고 표준편차 하나로 압축**

```
std_pass1 = std(1차 통과 유로 전체 유량)
std_pass2 = std(2차 통과 유로 전체 유량)
```

**발표 포인트**: 지표 정의 자체가 설계 판단 — AI와 대안을 비교 검토한 결과

---

## 슬라이드 18 — 데이터 확보 현황

**넣을 것**
- 확보 현황

| 구분 | 수량 |
|---|---|
| DOE 초기 실험점 | 90 점 |
| 적응샘플링 추가 | 105 점 |
| **누적 해석 데이터** | **195 점** |

- 확보된 설계공간 범위

| 항목 | 범위 |
|---|---|
| 방열핀 두께 / 개수 | 1.5 ~ 3.0 mm / 10 ~ 21 개 |
| 유로 갭 | 2.500 ~ 6.409 mm (가공 제약 하한까지 도달) |
| 차압 | 1,701 ~ 4,661 Pa |
| 최고온도 | 116.8 ~ 122.9 ℃ |
| 중량 | 4.90 ~ 5.24 kg |

- 데이터 구조

```
idx | 설계변수 9종 | 갭(종속값) | 목적함수 5종 | 제약조건 2종
```

**발표 포인트**: 설계공간 전 영역에 걸쳐 고르게 데이터 확보 → 다음 달 대리모델 학습의 입력

---

## 슬라이드 19 — 차월 계획

- 확보된 195점 기반 머신러닝 대리모델(GPR) 학습
- 예측값·실측값 비교를 통한 정확도 검증
- 설계변수별 민감도 산출 및 지배 변수 식별
- 격자수렴성시험(GCI)을 통한 해석 불확도 정량화

---

## 작성 시 유의사항

| 항목 | 내용 |
|---|---|
| 분량 배분 | 01(활용방안) 6매 · 02(설계변수) 5매 · 03(DOE 결과) 5매 — 산출물 명칭이 "활용방안 **및** DOE 결과"이므로 양쪽 균형 |
| 핵심 슬라이드 | **7·8번(활용 사례)** — 정성적 성과를 수치로 보여주는 유일한 장표. 가장 공들일 것 |
| | **18번(확보 현황)** — 이번 달 산출물의 실체. 수치는 발표 시점 최신값으로 갱신 |
| 도메인 설명 | 유로 형상 세부 설명은 최소화 (설계가이드 수록용이므로 방법론 위주) |
| 주의 | 슬라이드 12의 "이전 차수 데이터 211점"은 V4 기준값 — 인용 시 출처 명시 |
