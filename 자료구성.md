# 발표자료 구성 (작업용 — 계속 갱신)

> 대화하면서 정해지는 방향/구성을 이 파일에 계속 반영합니다. 최종 결론만 남기지 말고,
> 바뀐 이유도 짧게 남겨서 나중에 왜 이렇게 갔는지 추적 가능하게 합니다.

---

## 1. 발표 어젠다 (핵심 결론 — 절대 흔들리면 안 되는 축)

- 사외망에서 Claude / Claude Code로 **바이브코딩**해서 냉각판 열해석 자동화 파이프라인을 만들었다.
- 근데 SolidWorks·Icepak(AEDT)은 **사내망(폐쇄망)** 안에만 있어서, 짠 코드를 매번 **복붙으로 반입**하고,
  에러 나면 다시 사외망 가서 고치고 재반입하는 **왕복**을 반복해야 했다.
- **결론**: 이런 왕복의 비효율 속에서도 이 정도 자동화가 가능했다면, **사내망에 고도화된 AI 에이전트만
  있으면** 왕복 없이 더 쉽고 빠르게, 더 넓은 범위에서 툴 제어 자동화가 가능하다.
- **제안**: 사내망 AI 에이전트 도입 검토.

> ⚠ 기술적 디테일(크리깅/능동학습/자기검증 5종 등)은 위 결론을 뒷받침하는 "이 정도까지 vibe coding으로
> 됐다"는 근거로만 한 장 정도 압축해서 쓴다. 메인 서사가 아니다.

---

## 2. 배제하기로 한 것

| 대상 | 배제 이유 |
|---|---|
| 콜레그(hyunjin4459-coder)가 작성한 `thermal-ml-review` 딥다이브 문서 | 열해석 파이프라인 자체의 기술적 타당성 검증 리포트라, "망분리 때문에 왕복했다 → 사내망 AI 에이전트 필요"라는 이번 발표 어젠다와 결이 다름. 용어/강조점도 실제 하고 싶은 말과 거리가 있음 |
| Mermaid(md) 버전 아키텍처 다이어그램 | 자동 레이아웃이라 원본 이미지의 배치를 그대로 못 살림. HTML 수동 배치 버전으로 대체 |

---

## 3. 산출물 현황

| 산출물 | 형식 | 상태 | 파일 |
|---|---|---|---|
| 발표 PPT 초안 (6장) | .pptx | 1차 완료 | `사내망_AI에이전트_제안.pptx` |
| 아키텍처 다이어그램 (사외망↔사내망 파이프라인 상세) | .html | 라벨/화살표 정렬 수정 중 | `바이브코딩_아키텍처.html` |

### PPT 6장 구성
1. 타이틀 — "AI 바이브코딩, 사내망이 막고 있는 것"
2. 문제 — 손으로 하면 뭐가 괴로운가 (시간/편차/감)
3. 시도 — 사외망에서 Claude/Claude Code로 바이브코딩한 것
4. **핵심** — 사외망(Claude Code) ↔ 사내망(SolidWorks·Icepak) 복붙 왕복 다이어그램
5. 기술 압축 1장 — 806가지/34~185회/오차기준/3주 숫자만
6. 결론 — 사내망 AI 에이전트 도입 제안

### 아키텍처 다이어그램 구성
- Claude(AI) ↔ User: 바이브 코딩 요청 / Python 스크립트
- User → 사용자 워크스테이션(User Workstation)
  - VS Code (개발 환경): 코드 편집기 / 디버깅 / 인터프리터 설정
  - Python 실행환경: CPython 3.10 → PyAEDT / Pywin32
  - Solidworks (CAD): 파트·어셈블리 모델 / Equation Manager / STEP Export
  - Ansys AEDT (시뮬레이션): AEDT Core / Icepak Solver / AEDT API
- 연결: 코드↔명령(VS↔Python), 해석실행/해석상태(PyAEDT↔AEDT API),
  형상갱신요청(COM)/리빌드완료(Pywin32↔SolidWorks), STEP파일(SolidWorks↔AEDT API),
  디버그결과/오차방향(AEDT→VS Code 피드백 루프)

---

## 4. 진행 로그

- 콜레그 리뷰 문서 검토 → 어젠다 불일치 확인 → 배제 결정
- PPT 6장 초안 제작 (망분리 왕복 서사 중심)
- 아키텍처 원본 이미지(사용자 제공 PNG) 판독 → HTML로 재현
- 1차 수정: 폰트 크기 확대, 라벨 문구 3건 수정(해석실행/해석상태/STEP파일), 방향 명확화
- 2차 수정: 화살표가 텍스트를 가로지르는 문제 발견 → 라벨 위치 이동 / 배경 처리
- 2차 수정: "형상 갱신 요청(COM)" 라벨이 Pywin32가 아니라 한 줄 위(CPython 3.10 행)에
  잘못 정렬돼 있던 것 발견 → Pywin32 옆으로 재배치
- (진행 중) 사용자가 최신 수정본과 이전 버전 차이를 못 느낀다고 함 → 캐시/파일 버전 확인 요청한 상태

---

## 5. 열린 질문 / 다음에 정할 것

- [ ] 위 2차 수정이 실제로 반영된 게 맞는지 스크린샷으로 재확인
- [ ] PPT 6장 분량이 실제 발표 시간/청중에 맞는지 (늘릴지 줄일지)
- [ ] 아키텍처 다이어그램을 PPT 4번 슬라이드에 그대로 넣을지, 별도 첨부자료로 뺄지
